# Documentation Policy

Every coding action must belong to:

```text
Project → Phase → Component → Workstream → Tranche → Task
```

At the end of each tranche, update:

- implementation log;
- resume marker;
- affected specs/docs.
